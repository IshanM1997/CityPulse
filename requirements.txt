# Core
flask>=3.0.0
pandas>=2.0.0
requests>=2.31.0
numpy>=1.24.0

# Optional: install Apache Airflow to use the real Airflow scheduler
# The built-in scheduler (scheduler.py) works without Airflow
# apache-airflow>=2.8.0

# Optional: for real data sources
# pyarrow>=14.0.0   # for reading NYC TLC parquet files directly
